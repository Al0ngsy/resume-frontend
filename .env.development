# Local development — picked up by `next dev` (NODE_ENV=development)
NEXT_PUBLIC_LOCAL=true

# Server-only (NOT exposed to browser) — used by route handlers
API_URL=http://localhost:8000
API_KEY=dev

# Chat mode: "hidden" | "placeholder" | "live"
NEXT_PUBLIC_CHAT_MODE=live